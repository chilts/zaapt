%# ----------------------------------------------------------------------------
<%args>
    $zaapt
    $my_perms
</%args>
%# ----------------------------------------------------------------------------
<%init>
    $m->redirect('edit-r-list.html') unless exists $my_perms->{super};

    my $model = $zaapt->get_model('Account');
    my $accounts = $model->sel_account_all();

    my @accounts = map { { value => $_->{a_id}, text => $_->{a_username} } } @$accounts;
</%init>
%# ----------------------------------------------------------------------------
<h1>Add a Permission</h1>

<p><a href="edit-p-list.html">&laquo; Back to Permission List</a></p>

<h2>Permission: ?</h2>

<& /zaapt/common/form.mhtml, action => 'edit-p-ins.html', elements => [
        { type => 'text', field => 'Name', name => 'p_name', size => 40, value => '' },
        { type => 'textarea', field => 'Description', name => 'p_description', rows => 10, cols => 40, value => '' },
        { type => 'submit', field => 'Actions', value => 'Add Permission', cancel => 'edit-p-list.html' },
    ]
&>
%# ----------------------------------------------------------------------------
