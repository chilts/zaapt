%# ----------------------------------------------------------------------------
<%args>
    $_act       => undef
    $a_username => undef
    $a_email    => undef
    $t_token    => undef
</%args>
%# ----------------------------------------------------------------------------
<%init>
    # need to do some stuff here
    if ( $_act eq 'reset' ) {

    }
</%init>
%# ----------------------------------------------------------------------------
<& view-reset.render.mhtml,
    a_username => $a_username,
    a_email    => $a_email,
    t_token    => $t_token,
&>
%# ----------------------------------------------------------------------------
