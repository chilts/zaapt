%# ----------------------------------------------------------------------------
<%args>
    $entry
    $comments
</%args>
%# ----------------------------------------------------------------------------
<%init>
    return unless $entry->{b_comment};
</%init>
%# ----------------------------------------------------------------------------
<h2 id="comments">Comments</h2>
% if ( @$comments ) {
%     foreach my $comment ( @$comments ) {
<& comment.render.mhtml, comment => $comment &>
%     }
% } else {
<p>No comments yet.</p>
% }
%# ----------------------------------------------------------------------------
