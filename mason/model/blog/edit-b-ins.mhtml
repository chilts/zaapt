%# ----------------------------------------------------------------------------
<%once>
    use Zaapt::Utils::Valid;
    use Zaapt::Utils::Conv;
    # use Zaapt::Utils::Conv qw(to_bool remove_cr);
</%once>
%# ----------------------------------------------------------------------------
<%args>
    $zaapt
    $my_roles
    $b_name        => ''
    $b_title       => ''
    $b_description => ''
    $b_show        => ''
    $b_moderate    => ''
    $b_comment     => ''
    $b_trackback   => ''
    $_admin        => ''
    $_view         => ''
    $_edit         => ''
    $_publish      => ''
    $_comment      => ''
    $_trackback    => ''
</%args>
%# ----------------------------------------------------------------------------
<%once>
    use Zaapt::Utils::Valid;
    use Zaapt::Utils::Conv;
</%once>
%# ----------------------------------------------------------------------------
<%init>
    my $model = $zaapt->get_model('Blog');

    $m->redirect('edit-b-list.html') unless exists $my_roles->{super};

    my $err = [];

    unless ( Zaapt::Utils::Valid::is_valid_name($b_name) ) {
        push @$err, Zaapt::Utils::Valid::err();
    }

    unless ( Zaapt::Utils::Valid::is_non_negative_integer($b_show, 'Show') ) {
        push @$err, Zaapt::Utils::Valid::err();
    }

    # no Blog of this name should exist
    unless ( @$err ) {
        my $blog = $model->sel_blog_using_name({ b_name => $b_name });
        push @$err, "Blog name '$b_name' already exists." if defined $blog;
    }

    $b_moderate = Zaapt::Utils::Conv::to_bool( $b_moderate );
    $b_comment = Zaapt::Utils::Conv::to_bool( $b_comment );
    $b_trackback = Zaapt::Utils::Conv::to_bool( $b_trackback );
    $b_description = Zaapt::Utils::Conv::remove_cr( $b_description );

    unless ( @$err ) {
        $model->ins_blog({
            b_name        => $b_name,
            b_title       => $b_title,
            b_description => $b_description,
            b_show        => $b_show,
            b_moderate    => $b_moderate,
            b_comment     => $b_comment,
            b_trackback   => $b_trackback,
            _admin        => $_admin,
            _view         => $_view,
            _edit         => $_edit,
            _publish      => $_publish,
            _comment      => $_comment,
            _trackback    => $_trackback,
        });
        $m->redirect("edit-b-list.html");
        return;
    }

    my $roles = $zaapt->get_model('Account')->sel_all_roles();
    my @options = map { { value => $_->{r_id}, text => $_->{r_name} } } @$roles;
</%init>
%# ----------------------------------------------------------------------------
<h1>Add a Blog</h1>

<p><a href="edit-b-list.html">&laquo; Back to Blog List</a></p>

<h2>Blog: ?</h2>

<& /zaapt/common/err.mhtml, err => $err &>

<& /zaapt/common/form.mhtml, action => 'edit-b-ins.html', elements => [
        { type => 'text', field => 'Name', name => 'b_name', size => 40, value => $b_name },
        { type => 'text', field => 'Title', name => 'b_title', size => 40, value => $b_title },
        { type => 'textarea', field => 'Description', name => 'b_description', rows => 10, cols => 40, value => $b_description },
        { type => 'text', field => 'Show', name => 'b_show', size => 3, value => $b_show },
        { type => 'check', field => 'Moderate', name => 'b_moderate', checked => $b_moderate },
        { type => 'check', field => 'Comment', name => 'b_comment', checked => $b_comment },
        { type => 'check', field => 'Trackback', name => 'b_trackback', checked => $b_trackback },
        { type => 'select', field => 'Admin Role', name => '_admin', options => \@options, selected => $_admin },
        { type => 'note', value => "Note: Allows this user to delete this entire blog." },
        { type => 'select', field => 'View Role', name => '_view', options => \@options, selected => $_view },
        { type => 'note', value => "Note: Allows this user to view all blog entries." },
        { type => 'select', field => 'Edit Role', name => '_edit', options => \@options, selected => $_edit },
        { type => 'note', value => "Note: Allows this user to add, edit and delete blog entries and labels." },
        { type => 'select', field => 'Publish Role', name => '_publish', options => \@options, selected => $_publish },
        { type => 'note', value => "Note: Allows this user to publish blog entries (not yet implemented)." },
        { type => 'select', field => 'Comment Role', name => '_comment', options => \@options, selected => $_comment },
        { type => 'note', value => "Note: Allows this user to moderate comments." },
        { type => 'select', field => 'Trackback Role', name => '_trackback', options => \@options, selected => $_trackback },
        { type => 'note', value => "Note: Allows this user to moderate trackbacks." },
        { type => 'submit', field => 'Actions', value => 'Add Blog', cancel => 'edit-b-list.html' },
    ]
&>

%# ----------------------------------------------------------------------------
