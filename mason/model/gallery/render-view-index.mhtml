%# ----------------------------------------------------------------------------
<%args>
    $gallery
    $pictures
</%args>
%# ----------------------------------------------------------------------------
<h1><% $gallery->{g_title} | h %></h1>

<p><% $gallery->{g_description} | h %></p>

% if ( @$pictures ) {
%     foreach my $picture ( @$pictures ) {
<& render-view-picture-summary.mhtml, gallery => $gallery, picture => $picture &>
%     }
% } else {
<p>Currently, there are no pictures in this gallery.</p>
% }
%# ----------------------------------------------------------------------------
